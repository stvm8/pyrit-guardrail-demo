[pytest]
asyncio_mode = auto
timeout = 120

markers =
    smoke: Fast tests that run on every PR (subset of attacks, ~2 min)
    slow: Exhaustive tests for main branch merges only (multi-turn, TAP, Crescendo, ~15 min)

# Output
log_cli = true
log_cli_level = WARNING
